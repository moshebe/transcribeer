name: Release

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag (e.g. v0.2.0)'
        required: true
        type: string

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "ok"
