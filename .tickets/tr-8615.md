{
  "id": "tr-8615",
  "title": "Remove Python codebase",
  "status": "closed",
  "type": "chore",
  "priority": 2,
  "tests_passed": false,
  "created_at": "2026-04-16T14:47:21.668Z",
  "parent": "tr-ab5f",
  "deps": [
    "tr-73d0"
  ]
}

Delete all Python code and dependencies after Swift migration is verified.

Delete:
- transcribeer/ (Python package)
- tests/ (Python tests)
- pyproject.toml
- uv.lock

Update:
- Makefile: remove `uv sync` from `dev` target, remove Python references
- .gitignore: remove .venv, __pycache__ entries
- README.md: update installation (no Python/pip/venv), document model download on first run, update config.toml docs

## Acceptance Criteria

- `make dev` works with zero Python installed
- No Python files remain in repo (except gui/.build caches)
- README accurately describes Swift-only installation
- config.toml docs updated with new `model` field, removed Python-only fields
